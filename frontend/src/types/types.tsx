export type Status = {
  status: string;
};
export type Dogs = {
  dog: string;
};

export type User = {
  picture: string;
  name: string;
  email: string;
  username: string;
  age: string;
  alt: string;
};
